interface ITheme {
    bgColor: string;
    navLeftColor: string;
    navTopColor: string;
    successColor: string;
    failedColor: string;
}

interface ILink {
    id: number;
    path: string;
    name: string;
}